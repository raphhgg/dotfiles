version = 1
id = "project-learnings-review"
kind = "cron"
name = "Project Learnings Review"
prompt = "Read `$HOME/agent-standards/docs/references/watched-product-repos.md` and the shared memory artifacts in `$HOME/.codex/memories`, especially `MEMORY.md` and the most relevant rollout summaries for watched repos touched in the last 7 days. Run this review once as a cross-project synthesis, not once per repo. Treat shared memory as the primary source for already-known durable learnings, user steering, and repeated repo-specific truth. Only inspect raw recent Codex sessions when shared memory is missing, ambiguous, or clearly stale for a watched repo. Attribute a session or memory entry to a project only when cwd, rollout/worktree path, or repeated repo-specific references clearly match that repo. For each watched repo, append only net-new, durable, reusable learnings to `.codex/learnings.md` when there is real evidence that is not already captured in shared memory or the repo-local learnings file; do not create or keep boilerplate files when there are no qualifying additions. Do not retain one-off implementation details, temporary file paths, speculative ideas, branch-specific facts, or proposals that were not implemented or validated. After scanning all watched repos, open exactly one inbox item that summarizes genuinely new repeated themes across projects that are still missing from `$HOME/agent-standards`, including a short prioritized list with likely target docs, skills, routing, or missing reference files. If the watched-repo registry itself looks stale or inconsistent with observed active repos or shared memory coverage, call that out explicitly in the single inbox item."
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=MO;BYHOUR=8;BYMINUTE=0"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/raphh/.codex"]
created_at = 1775922456610
updated_at = 1776066699954
