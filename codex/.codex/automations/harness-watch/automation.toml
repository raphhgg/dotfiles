version = 1
id = "harness-watch"
kind = "cron"
name = "Harness Watch"
prompt = "Read `$HOME/agent-standards/docs/references/reference-repos.md` and `$HOME/agent-standards/docs/references/watched-product-repos.md`. Compare the tracked upstream reference topics against the current harness in `$HOME/agent-standards`. Also read `.codex/learnings.md` from the watched product repos when present. Run this review once as a single harness-wide synthesis, not once per repo or cwd. Look for updates to existing topics, stale or missing guidance, routing drift, missing workflow contracts, missing skills, and missing reference files that should exist in the harness. Prioritize only the strongest gaps; do not pad the output with weak or speculative topics. Open exactly one inbox item with a ranked list of proposals, confidence, and likely target files in `agent-standards`. If coverage is mostly good, say so and keep the list short. If the watched-repo registry or reference-topic registry is drifting, include that as an explicit proposal rather than scattering repo-biased observations."
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=MO;BYHOUR=9;BYMINUTE=0"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/raphaelgrau/.codex"]
created_at = 1775917402597
updated_at = 1776066123355
