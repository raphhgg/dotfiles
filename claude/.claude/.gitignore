# Sensitive credentials - NEVER commit
.credentials.json
.secrets

# Local overrides (machine-specific)
settings.local.json

# Session and cache files
history.jsonl
shell-snapshots/
statsig/
todos/
ide/
projects/
downloads/

# Plugins repos (managed separately)
plugins/repos/
